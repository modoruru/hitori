name: hitori
version: $version
main: su.hitori.plugin.CorePlugin
api-version: '1.21'
dependencies:
  server:
    CommandAPI:
      load: AFTER
      required: true
    SkinsRestorer:
      load: BEFORE
      required: false